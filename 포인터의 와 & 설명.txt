포인터의 *와 & 설명
* 연산자는 포인터 변수가 가리키는 메모리 주소에 저장된 값을 반환합니다. 반면, & 연산자는 변수의 메모리 주소를 반환합니다.

예를 들어, int a = 5; 라는 코드에서 a는 정수형 변수이고, 값은 5입니다. int *p = &a; 라는 코드에서 p는 포인터 변수이고, a의 주소를 저장합니다. 이 경우, *p는 a의 값과 같으므로 5가 됩니다. 또한, &a는 a의 메모리 주소를 반환합니다.